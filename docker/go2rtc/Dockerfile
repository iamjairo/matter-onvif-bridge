FROM alexxit/go2rtc:1.9.7

# go2rtc uses the tracked config file at docker/go2rtc/go2rtc.yaml,
# mounted at runtime by docker-compose to /config/go2rtc.yaml.
# The official image already exposes ports 1984 (API), 8555 (WebRTC), 8554 (RTSP)
